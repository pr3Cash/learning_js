// let map = new Map();

// map.set('a','1');
// map.set('b',2);
// map.set("c",3);
// map.set(4,"d");

// let tal = map.entries();
// console.log(tal);

//objects as keys

let rMp = new Map([
    ['cu',50],
    ['to',35],
    ['on',5]
]);

for(let ent of rMp){
alert(ent);
}